repositories {
    mavenCentral()
}

plugins {
    //`kotlin-dsl`
    //kotlin(BuildPlugins.ANDROID_APPLICATION).version(BuildDependencyVersions.ANDROID_APP_LIBRARY_VERSION).apply(false)
    //kotlin(BuildPlugins.ANDROID_LIBRARY).version(BuildDependencyVersions.ANDROID_APP_LIBRARY_VERSION).apply(false)
}

/*
tasks.register("clean", Delete::class) {
    delete(rootProject.buildDir)
}
*/

/*
dependencies {
    //implementation(project(BuildModules))
    addDependenciesForCompose()
}
*/
